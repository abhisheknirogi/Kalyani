export default function Hero() {
  return (
    <section className="hero">
      <div className="container">
        {/* contact button at top of hero */}
        <a href="#contact" className="cta" aria-label="Contact us">
          Contact Us
        </a>

        <h1> Glow n'Bride</h1>
        <p>Beauty, one brush stroke at a time.</p>
      </div>
    </section>
  );
}
